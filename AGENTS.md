# WordCraft Agent Guidelines

## 1. UI 与文案准则
- **UI 文案极简原则**：严禁在 UI 界面中使用啰嗦口水话、冗长说明或套话；保持高信息纯度、低视觉噪音，适配阿斯伯格儿童认知习惯。
- **双区域与具体问题推进**：左侧图片区 + 右侧构建区，每一步严格以具体问题为驱动。

## 2. Git 操作准则
- **在执行任何 `git commit` 和 `git push` 前，必须先向用户询问并列出本次改动概要，获得用户确认后方可执行。**
